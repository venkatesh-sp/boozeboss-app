/*
 * ApproveCredits Messages
 *
 * This contains all the text for the ApproveCredits container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ApproveCredits';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ApproveCredits container!',
  },
});
