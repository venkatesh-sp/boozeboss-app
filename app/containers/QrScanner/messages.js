/*
 * QrScanner Messages
 *
 * This contains all the text for the QrScanner container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.QrScanner';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the QrScanner container!',
  },
});
