/*
 * SignupPage Messages
 *
 * This contains all the text for the SignupPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AcceptOrders';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AcceptOrders container!',
  },
});
