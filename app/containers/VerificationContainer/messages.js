/*
 * VerificationContainer Messages
 *
 * This contains all the text for the VerificationContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.VerificationContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the VerificationContainer container!',
  },
});
