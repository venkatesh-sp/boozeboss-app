/*
 * ApproveFreeDrink Messages
 *
 * This contains all the text for the ApproveFreeDrink container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ApproveFreeDrink';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ApproveFreeDrink container!',
  },
});
