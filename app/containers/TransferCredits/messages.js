/*
 * TransferCredits Messages
 *
 * This contains all the text for the TransferCredits container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TransferCredits';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TransferCredits container!',
  },
});
