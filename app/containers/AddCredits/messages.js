/*
 * AddCredits Messages
 *
 * This contains all the text for the AddCredits container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddCredits';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddCredits container!',
  },
});
