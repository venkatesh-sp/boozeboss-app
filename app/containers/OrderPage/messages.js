/*
 * OrderPage Messages
 *
 * This contains all the text for the OrderPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OrderPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OrderPage container!',
  },
});
